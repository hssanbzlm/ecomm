import React from "react";

function NotFound() {
  return (
    <div
      style={{
        width: "100%",
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "sans-serif",
      }}
    >
      PAGE NOT FOUND
    </div>
  );
}

export default NotFound;
